{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.internal.ts",
    "build": "vite --config vite.config.internal.ts build",
    "inspect:build": "vite --config vite.config.internal.ts build --watch",
    "inspect:serve": "live-server --port=3000 --host=localhost --no-browser .vite-inspect",
    "preview": "vite --config vite.config.internal.ts preview"
  },
  "devDependencies": {
    "live-server": "1.2.2",
    "magic-string": "^0.30.10",
    "serve": "^14.2.3",
    "vite": "^5.2.0",
    "vite-plugin-inspect": "^0.8.4"
  }
}
